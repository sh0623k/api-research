package resolvers

/*
func convertTodoMapToSlice(todosMap map[string]*model.Todo) []*model.Todo {
	todosSlice := make([]*model.Todo, 0)
	for _, todo := range todosMap {
		todosSlice = append(todosSlice, todo)
	}
	return todosSlice
}
*/
